local config = {
  enabled = true;
  random = true;
  debug_reload = false;
}

local configDesc = {
  random = "Randomize familiar cosmetic on run start",
  debug_reload = "Meant for dev/debug purposes";
}

return config, configDesc