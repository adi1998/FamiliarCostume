local config = {
  enabled = true;
  random = true;
  debug_reload = false;
}

local configDesc = {
  random = "Randomize familiar cosmetic on run start(can be toggled ingame too)",
  debug_reload = "Meant for dev/debug purposes";
}

return config, configDesc